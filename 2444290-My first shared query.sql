create table members 
    ( 
        id INT,
        names VARCHAR(50),
        phone_numb INT,
        spec_num INT foreign,
    );
create table spec 
    {
        spec_num primary,
        num_of_students,
    }